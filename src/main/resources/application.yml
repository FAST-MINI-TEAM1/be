
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://127.0.0.1:3311/miniProject
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: miniProject
    password: miniProject1!@
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create # create or update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    properties:
      hibernate:
        format_sql: true
    # defer-datasource-initialization: true # ????? ?? ?? data.sql


---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database: h2
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true